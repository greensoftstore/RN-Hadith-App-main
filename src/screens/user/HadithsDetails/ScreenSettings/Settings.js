import { moduleNames } from "constantsConfiguration/enums/modules";

const settings = {
    title: {
        main: moduleNames.HADITHSDETAILS,
    },
    label: moduleNames.HADITHSDETAILS,
}

export default settings;