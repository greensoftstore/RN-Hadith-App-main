import { moduleNames } from "constantsConfiguration/enums/modules";

export const settings = {
    title: {
        main: 'AppIntro',
    },
    label: moduleNames.APPINTRO,
}

export default ScreenSettings = {
    // MAIN
    settings: settings,
}