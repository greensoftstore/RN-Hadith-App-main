import { moduleNames } from "constantsConfiguration/enums/modules";

const settings = {
    title: {
        main: moduleNames.BUNDLEDOWNLOAD,
    },
    label: moduleNames.BUNDLEDOWNLOAD,
}

export default settings;