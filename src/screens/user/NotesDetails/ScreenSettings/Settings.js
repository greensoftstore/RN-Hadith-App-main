import { moduleNames } from "constantsConfiguration/enums/modules";

const settings = {
    title: {
        main: moduleNames.NOTESDETAILS,
    },
    label: moduleNames.NOTESDETAILS,
}

export default settings;