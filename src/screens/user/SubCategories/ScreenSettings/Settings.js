import { moduleNames } from "constantsConfiguration/enums/modules";

const settings = {
    title: {
        main: moduleNames.SUBCATEGORY,
    },
    label: moduleNames.SUBCATEGORY,
}

export default settings;