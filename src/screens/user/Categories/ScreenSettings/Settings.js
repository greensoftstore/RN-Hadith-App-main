import { moduleNames } from "constantsConfiguration/enums/modules";

const settings = {
    title: {
        main: moduleNames.CATEGORY,
    },
    label: moduleNames.CATEGORY,
}

export default settings;