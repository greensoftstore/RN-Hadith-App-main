import { moduleNames } from "constantsConfiguration/enums/modules";

const settings = {
    title: {
        main: moduleNames.HADITHS,
    },
    label: moduleNames.HADITHS,
}

export default settings;