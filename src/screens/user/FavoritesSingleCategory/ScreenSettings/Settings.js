import { moduleNames } from "constantsConfiguration/enums/modules";

const settings = {
    title: {
        main: moduleNames.FAVORITES
    },
    label: moduleNames.FAVORITES,
}

export default settings;