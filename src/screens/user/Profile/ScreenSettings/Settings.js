import { moduleNames } from "constantsConfiguration/enums/modules";

const settings = {
    title: {
        main: moduleNames.PROFILE,
    },
    label: moduleNames.PROFILE,
}

export default settings;