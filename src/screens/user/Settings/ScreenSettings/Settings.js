import { moduleNames } from "constantsConfiguration/enums/modules";

const settings = {
    title: {
        main: moduleNames.SETTINGS,
        font: 'fontSize',
        theme: 'darkTheme'
    },
    label: moduleNames.SETTINGS,
}

export default settings;