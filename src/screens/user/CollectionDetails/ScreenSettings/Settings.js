import { moduleNames } from "constantsConfiguration/enums/modules";

const settings = {
    title: {
        main: moduleNames.COLLECTIONDETAILS,
    },
    label: moduleNames.COLLECTIONDETAILS,
}

export default settings;