import { moduleNames } from "constantsConfiguration/enums/modules";

const settings = {
    title: {
        main: 'Reset Password'
    },
    label: moduleNames.RESETPASSWORD,
}

export default settings;