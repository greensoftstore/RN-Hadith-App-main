import { moduleNames } from "constantsConfiguration/enums/modules";

const settings = {
    title: {
        main: 'Registration'
    },
    label: moduleNames.REGISTRATION,
}

export default settings;